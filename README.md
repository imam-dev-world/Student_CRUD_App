# Student_CRUD_App
Student-CRUD is a RESTful backend application built with Spring Boot 4.0.6 that provides basic Create, Read, Update, and Delete (CRUD) operations for managing student records. It uses Spring Data JPA to interact with a relational database.
